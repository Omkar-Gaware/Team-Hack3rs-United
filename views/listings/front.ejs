<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elderly Care Companion</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }
        body, html {
            height: 100%;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            text-align: center;
            position: relative;
            background: url('img1.jpg') no-repeat center center fixed;
            background-size: cover;
            animation: bgSlide 12s infinite;
        }

        /* Background slideshow animation */
        @keyframes bgSlide {
            0% {
                background-image: url('img1.jpg');
            }
            33% {
                background-image: url('img2.jpg');
            }
            66% {
                background-image: url('img3.jpg');
            }
            100% {
                background-image: url('img1.jpg');
            }
        }

        /* Content Styling */
        .container {
            max-width: 800px;
            background: rgba(85, 85, 85, 0.5);
            padding: 30px;
            border-radius: 15px;
        }

        h2 {
            font-size: 55px;
            font-weight: bold;
            color: rgb(15, 14, 14);
        }

        p {
            font-size: 22px;
            margin-top: 10px;
            opacity: 0.9;
        }

        .btn-container {
            margin-top: 25px;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            padding: 15px 30px;
            font-size: 20px;
            font-weight: bold;
            color: #333;
            background: white;
            border-radius: 30px;
            text-decoration: none;
            transition: 0.3s ease-in-out;
        }

        .btn:hover {
            background: #f1f1f1;
        }
    </style>
</head>
<body>

    <div class="container">
        <h2>Welcome to Elderly Care Companion</h2>
        <p>Helping seniors stay healthy, safe, and connected.</p>
        <div class="btn-container">
            <a href="login.html" class="btn">🚀 Get Started</a>
        </div>
    </div>

</body>
</html>